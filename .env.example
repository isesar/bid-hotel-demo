# Booking API proxy target (server-only; used by /api/booking/*)
BOOKING_API_URL=""

# Home page background video (desktop only) — public CDN URLs (e.g. Vercel Blob)
# MP4 is required (universal fallback); WEBM and POSTER are optional.
NEXT_PUBLIC_BG_VIDEO_POSTER=""
NEXT_PUBLIC_BG_VIDEO_MP4=""
NEXT_PUBLIC_BG_VIDEO_WEBM=""
